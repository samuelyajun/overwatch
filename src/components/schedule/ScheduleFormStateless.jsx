import React, { PropTypes } from 'react';
import { Link } from 'react-router';
import Button from '../common/Button.jsx';

const ScheduleFormStatless = ({ }) => {
    return (
        <div>
            <h1>Hello</h1>
        </div>
    );
};

ScheduleFormStatless.propTypes = {
};

export default ScheduleFormStatless;